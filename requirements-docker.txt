# Minimal runtime requirements for Docker API image
# Heavy dev/testing deps (mypy, pre-commit, etc.) are excluded

# Core ML
scikit-learn>=1.4.0
lightgbm>=4.3.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.3.0

# Feature engineering
scipy>=1.12.0

# Experiment tracking
mlflow[skinny]>=2.12.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
email-validator>=2.0.0

# HTTP client (for Evidently background capture)
httpx>=0.27.0

# Monitoring — Prometheus metrics
prometheus-client>=0.20.0
